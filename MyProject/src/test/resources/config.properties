# Base URL for your application
baseUrl=https://www.saucedemo.com/

# Browser options: chrome, firefox, chromium
browser=chrome

# Headless mode: true or false
headless=false

# Default timeout in seconds for waits
timeout=30

# Example test credentials (can be moved to separate test data later)
username=standard_user
password=secret_sauce